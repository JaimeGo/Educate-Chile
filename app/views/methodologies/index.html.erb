<div class="container-fluid">
  <h1>Metodologías</h1>

  <div class="row">
    <% @methodologies.each do |methodology| %>
      <div class="col-sm-6" onclick=>
        <div class="card">
          <div class="card-block">
            <h4 class="card-title"><%= methodology.name %></h4>
            <p class="card-text"><%= methodology.description %></p>
            <a  href='<%= methodology.link %>' class="btn btn-primary">Ver</a>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>